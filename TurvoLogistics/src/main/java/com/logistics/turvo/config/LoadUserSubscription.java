package com.logistics.turvo.config;
/*
import java.util.List;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationListener;
import org.springframework.context.event.ContextRefreshedEvent;
import org.springframework.stereotype.Component;
import com.logistics.turvo.model.Users;
import com.logistics.turvo.repository.UserReposiroty;
import com.logistics.turvo.service.NotificationHelper;

@Component
public class LoadUserSubscription implements ApplicationListener<ContextRefreshedEvent> {

	@Autowired
	private UserReposiroty userRepo;
	
	@Autowired
	private NotificationHelper helper;
	
	
	@Override
	
	public void onApplicationEvent(ContextRefreshedEvent event) {
		
	List<Users> users = userRepo.findAll();
	for(Users user : users){
		helper.subscribe(user);
	}
	}

} */
